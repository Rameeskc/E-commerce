// wish list toggling

async function toggleWishlist(event){
    event.preventDefault();

    const heart = event.currentTarget.queryselector(".favIcon")
    const productId = event.currentTarget.getAttribute("data-product-id")
}